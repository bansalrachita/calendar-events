import React, { Component } from "react";
import PropTypes from "prop-types";

class CalenderContainer extends Component {
  constructor(props) {
    super(props);
  }

  render() {
    return <div>hi there! I'm ready.</div>;
  }
}

export default CalenderContainer;
